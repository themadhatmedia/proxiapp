import 'package:flutter/material.dart';

/// Avatar file save uses `dart:io`, which is not available on web.
Future<String?> cropProfilePictureFromPath(
  BuildContext context,
  String sourcePath,
) async =>
    null;
